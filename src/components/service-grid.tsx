import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { services } from "@/lib/site-data";

export function ServiceGrid({ limit }: { limit?: number }) {
  return <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{services.slice(0, limit).map((service, index) => { const Icon = service.icon; return <Card key={service.href} className="group relative overflow-hidden border-white bg-white shadow-[0_26px_75px_-50px_rgba(8,22,43,.48)] transition-all duration-500 hover:-translate-y-2 hover:border-primary/15 hover:shadow-[0_38px_85px_-45px_rgba(223,0,122,.28)]"><span className="absolute right-5 top-4 font-editorial text-6xl font-medium text-slate-100 transition-colors group-hover:text-primary/8">0{index + 1}</span><div className={`absolute inset-x-0 top-0 h-1 origin-left scale-x-0 transition-transform duration-500 group-hover:scale-x-100 ${service.tone === "pink" ? "bg-primary" : "bg-sky-400"}`} /><CardContent className="relative p-7 sm:p-8"><div className={`mb-10 grid size-14 place-items-center rounded-[1.25rem] ring-8 ring-white ${service.tone === "pink" ? "bg-primary/10 text-primary" : "bg-sky-100 text-sky-700"}`}><Icon className="size-6" /></div><h3 className="text-xl font-bold tracking-tight">{service.title}</h3><p className="mt-3 min-h-20 text-sm leading-6 text-muted-foreground">{service.description}</p><Link href={service.href} className="mt-8 inline-flex items-center gap-2 text-sm font-bold text-primary">Mehr erfahren <span className="grid size-8 place-items-center rounded-full bg-primary/8 transition-all group-hover:bg-primary group-hover:text-white"><ArrowUpRight className="size-4 transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" /></span></Link></CardContent></Card> })}</div>;
}
